{% extends 'base.html.twig' %}

{% set successMessages = app.flashes('success') %}

{% block body %}

<section class="contact-hero">
  <div class="contact-hero-content">
    <h1>Conception de votre événement</h1>
    <p>Parlez-moi de votre projet culinaire — je vous réponds rapidement.</p>
  </div>
</section>

<section class="contact-section">
  <div class="contact-flex">
    <!-- Bloc gauche : photo + texte -->
    <div class="contact-left">
      <img src="/img/chef.jpg" alt="Photo du chef">
      <div class="contact-left-content">
        <h2>Entrons en contact</h2>
        <p>
          Que ce soit pour un dîner privé, un mariage ou un événement d’entreprise, 
          je serai ravi d’échanger avec vous pour créer une expérience sur mesure.
        </p>
         <p class="contact-alt">
            Vous pouvez aussi me contacter sur Instagram.
        </p>
        <div class="contact-details">
          <p>📍 Basé à Rochefort — Déplacements sur demande</p>
          {# <p>📧 <a href="mailto:contact@tonsite.fr">nicolas@tonsite.fr</a></p> #}
          <p>📸 <a href="https://www.instagram.com/toncompte" target="_blank">Instagram</a></p>
        </div>
      </div>
    </div>

    <!-- Bloc droit : formulaire -->
    <div class="contact-form-container">
      <h3>Envoyez-moi un message</h3>
      {{ form_start(form, {'attr': {'class': 'contact-form'}}) }}
        <div class="form-group">
          <label for="name">Nom complet</label>
          {{ form_widget(form.name) }}
        </div>
         <div class="error">
            {{ form_errors(form.name) }}
          </div>

        <div class="form-group">
          <label for="email">Adresse e-mail</label>
          {{ form_widget(form.email) }}
        </div>
        <div class="error">
            {{ form_errors(form.email) }}
        </div>

        <div class="form-group">
          <label for="phone">Téléphone</label>
          {{ form_widget(form.phone) }}
        </div>
        <div class="error">
            {{ form_errors(form.phone) }}
        </div>

        <div class="form-group">
          <label for="message">Votre message</label>
          {{ form_widget(form.message) }}
        </div>
        <div class="error">
            {{ form_errors(form.message) }}
        </div>
        {{ form_rest(form) }}
        <button type="submit" class="btn-submit">Envoyer le message</button>
        {% for message in successMessages %}
          <div class="success-message" id="flashmessage">{{ message }}</div>
        {% endfor %}
      {{ form_end(form) }}
      
    </div>
  </div>
</section>


{# <div class="home contact_header flex flex-column align-center flex-center" style="background-image: url('/img/contactpage.jpg');">

  <h2 class="title text_shadow_light mb20 mt0">Conception de votre événement</h2>
  <div class="contact_header_text">
    Parlez-moi de votre projet.
    Je vous répondrai rapidement <br>
    
  </div>
</div>

<div class="section-contact-form">
  <div class="contact-form-img">
    <img src="/img/chef.jpg" alt="un chef heureux" style="width:100%;border-radius:5px;">
  </div>
  <div class="contact-form-container" style="">
    {% for message in successMessages %}
    <div class="success-message" id="flashmessage">{{ message }}</div>
    {% endfor %}

    <div style="width:100%; height: fit-content;">
      <div class="contact-form-wrapper" style="border-bottom: none;">
        <form method="POST" id="contact-form" class="flex flex-column" style="padding: 25px;">
          <div class="error">
            {{ form_errors(form.name) }}
          </div>
          {{ form_widget(form.name, {'attr': {'class': 'form-control', 'placeholder': 'Votre nom'}}) }}

          <div class="error">
            {{ form_errors(form.phone) }}
          </div>
          {{ form_widget(form.phone, {'attr': {'class': 'form-control', 'placeholder': 'Téléphone'}}) }}

          <div class="error">
            {{ form_errors(form.email) }}
          </div>
          {{ form_widget(form.email, {'attr': {'class': 'form-control', 'placeholder': 'Email'}}) }}

          <div class="error">
            {{ form_errors(form.message) }}
          </div>
          {{ form_widget(form.message, {'attr': {'class': 'form-control', 'placeholder': 'Votre Message', 'rows': '6'}}) }}

          <div class="form-submit" style="margin-top: 30px;">
            {{ form_rest(form) }}
            <button type="submit" class="submit-btn">Envoyer</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</div> #}

{% endblock %}